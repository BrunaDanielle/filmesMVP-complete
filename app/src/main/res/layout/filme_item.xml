<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardBackgroundColor="@android:color/white"
    android:elevation="3dp"
    app:cardCornerRadius="8dp">
    <!--xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:layout_margin="8dp"
    card_view:cardUseCompatPadding="true"> -->

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="280dp"
            android:id="@+id/filme_thumb"
            android:scaleType="fitXY"/>

        <TextView
            android:layout_below="@+id/filme_thumb"
            android:id="@+id/filme_titulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="Título filme"
            android:layout_centerHorizontal="true"
            android:textAlignment="center"
            />
    </RelativeLayout>

   <!-- <ImageView
        android:id="@+id/filme_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        tools:src="@drawable/ic_insert_photo_black_48px" />

    <TextView
        android:id="@+id/filme_titulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|left"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"
        android:padding="16dp"
        android:background="@color/colorTranslucentBlack"
        tools:text="Star Wars EP IV - A New Hope" /> -->

</androidx.cardview.widget.CardView>